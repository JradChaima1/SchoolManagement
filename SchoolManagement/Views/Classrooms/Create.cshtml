@model School.Core.Models.Classroom

@{
    ViewData["Title"] = "Add New Classroom";
}

<div class="page-header">
    <h2><i class="bi bi-plus-circle-fill"></i> Add New Classroom</h2>
    <p class="mb-0">Enter classroom information</p>
</div>

<div class="row">
    <div class="col-md-8 mx-auto">
        <div class="card card-custom">
            <div class="card-header card-header-custom">
                <i class="bi bi-pencil-square"></i> Classroom Information
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="Name" class="form-label">Classroom Name</label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., 10A, 11B" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="GradeLevel" class="form-label">Grade Level</label>
                            <select asp-for="GradeLevel" class="form-select" required>
                                <option value="">Select Grade</option>
                                <option value="1">Grade 1</option>
                                <option value="2">Grade 2</option>
                                <option value="3">Grade 3</option>
                                <option value="4">Grade 4</option>
                                <option value="5">Grade 5</option>
                                <option value="6">Grade 6</option>
                                <option value="7">Grade 7</option>
                                <option value="8">Grade 8</option>
                                <option value="9">Grade 9</option>
                                <option value="10">Grade 10</option>
                                <option value="11">Grade 11</option>
                                <option value="12">Grade 12</option>
                            </select>
                            <span asp-validation-for="GradeLevel" class="text-danger"></span>
                        </div>
                        <div class="col-md-12">
                            <label asp-for="Capacity" class="form-label">Capacity</label>
                            <input asp-for="Capacity" type="number" class="form-control" min="1" max="100" required />
                            <span asp-validation-for="Capacity" class="text-danger"></span>
                            <small class="text-muted">Maximum number of students</small>
                        </div>
                    </div>
                    
                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary-custom">
                            <i class="bi bi-check-circle"></i> Save Classroom
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-x-circle"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
